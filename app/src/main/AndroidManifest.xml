<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="itesm.com.tecstore">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeCart">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogIn" />
        <activity android:name=".Register" />
        <activity android:name=".Register" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".Feed" />
        <activity
            android:name=".Feed_TecStore"
            android:label="@string/title_activity_feed__tec_store"
            android:theme="@style/AppTheme" />
        <activity android:name=".Menu_tecstore" />
        <activity android:name=".Search" />
        <activity android:name=".ArticleList"></activity>
    </application>

</manifest>